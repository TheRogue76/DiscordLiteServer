version: v2
modules:
  - path: .
    name: buf.build/parsascontentcorner/discordliteserver
lint:
  use:
    - STANDARD
  except:
    - RPC_RESPONSE_STANDARD_NAME  # Allow MessageEvent for streaming events
breaking:
  use:
    - FILE
  ignore_unstable_packages: true
